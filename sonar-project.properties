# Required metadata
sonar.projectKey=66c92708eddca368df1b2a2cbaa8cbe25b9759fd
sonar.organization=thanujad00

# Path to source directories
sonar.sources=.

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# Exclude patterns (adjust as needed)
sonar.exclusions=**/*.test.js,**/*.spec.js,**/node_modules/**,**/coverage/**

# Language specific settings
# sonar.python.coverage.reportPaths=coverage.xml  # Uncomment if Python project with coverage
# sonar.javascript.lcov.reportPaths=coverage/lcov.info  # Uncomment if JS project with Jest coverage

# SonarCloud host URL
sonar.host.url=https://sonarcloud.io